# infrastructure

Drizzle repositories, Better Auth wiring, and the real `Clock` / `IdGenerator` / `PickupCodeGenerator`.

Sits below the domain and points **up**: it implements ports declared in `domain` and `application`.
